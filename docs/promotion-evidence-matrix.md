# Promotion Evidence Matrix

This matrix tracks the shared promotion gate for issues #21-#29. A row is **not promoted**
until implementation, compatibility, restart, diagnostics, and retained same-commit evidence
are all present. Local tests prove correctness slices; they do not prove hosted client support,
representative capacity, latency, or production readiness.

| Issue | Surface and current evidence | Missing closure evidence | Status |
| --- | --- | --- | --- |
| #12 | The selected surface is feedback-informed scalar read selection plus vectorized-to-merge inner/left equi-join switching. Deterministic fixed/evaluation profile comparison covers exact results, ordering, pagination, transaction visibility, errors, paths, fallback diagnostics, storage reads, candidates, memory, workers, and restart rollback. Main CI retains `cassie-adaptive-profile-<commit>`. | Candidate expansion, other operator pairs, automatic profile selection, and any default enablement remain outside this promotion. | Selected opt-in surface promoted; broader adaptive planning remains Experimental and disabled by default. |
| #21 | Named-client catalog subset promoted: the exact `information_schema.tables` and `information_schema.columns` columns, ordering contract, restart and rename/drop lifecycle, `42P01` failure, and unsupported parity boundary are documented in `docs/catalog-support.md`. Retained sqlx 0.8.3 run 32503127504 and Diesel 2.2.6 run 32508304197 passed. | pgAdmin, DBeaver, other virtual views, undocumented columns, OID parity, extensions, and PostgreSQL internals remain outside the Stable subset. | Named-client subset promoted; remaining virtual catalogs stay Experimental. |
| #22 | Limited procedures and `CALL` have a published one-statement contract, argument binding, restart hydration, command metadata, deterministic unsupported-syntax coverage, recursion and transaction-control rejection, plus a retained `tokio-postgres` create/call/read workflow. | No gap for the named subset; broader procedural-language or business-logic behavior requires a separate product decision. | Stable only for the contract in `docs/procedure-support.md`; PL/pgSQL, triggers, dynamic SQL, transaction control, recursion, and business logic remain unsupported. |
| #23 | Rollup/retention lifecycle, stale fallback, maintenance debt, restart, and metrics coverage exist in the `time_series_rollups` and `time_series_retention` modules of `tests/domain_models.rs` and related recovery suites. | Retained idempotency and operator-window benchmark evidence at declared fixture sizes, including dependent freshness. | Not promoted. |
| #24 | HNSW has been Stable since `e9743a67`. Option validation, persistence, mutation safety, exact rerank, fallback, dimensions, deterministic publication recovery, and recall checks are linked from [HNSW Release Evidence](hnsw-release-evidence.md). The Tier 3 and Tier 5 artifact contract now rejects missing recall/configuration evidence or recall below 0.90. | Exact-head native-disk artifacts remain required before closing the readiness record; hosted provider availability remains outside the HNSW contract. | Stable functional classification preserved; readiness evidence in progress. |
| #25 | IVFFlat has been Stable since `e9743a67`. Training persistence, refresh, list validation, fallback, restart, deterministic publication recovery, and recall checks are linked from [IVFFlat Release Evidence](ivfflat-release-evidence.md). Tier 3 and Tier 5 artifact validation rejects missing recall/configuration evidence, changed list/probe/training profiles, or recall below 0.90. | Exact-head native-disk artifacts remain required before closing the readiness record; hosted provider availability remains outside the IVFFlat contract. | Stable functional classification preserved; readiness evidence in progress. |
| #26 | Provider-specific status contradiction resolved: deterministic local embeddings are Stable, while OpenAI, OpenAI-compatible, TEI, Ollama, Voyage, and Cohere remain Experimental. Provider controls, response bounds, retries, cancellation, dimensions, models, and self-hosted protocols are covered by the embedding modules in `tests/vector_embeddings.rs` and `rest_embeddings` in `tests/rest.rs`. Mock-provider auth and HTTP 429 evidence retained by those consolidated modules. | Hosted availability, quotas, account authorization, model availability, and representative service latency remain provider/operator responsibilities. | Local provider promoted; remote providers remain Experimental without external availability claims. |
| #27 | The selected surface is fixed-duration time-series indexing with positive integer minute, hour, or day widths. `time_series_indexes`, `time_series_index_completeness`, `specialized_query_controls`, and the 15-minute/1-hour/1-day differential matrix cover boundaries, pre-epoch timestamps, partitions, pagination, parameters, lifecycle metadata, corruption/fallback, cancellation, memory, workers, and diagnostics. Tier 3 registers one native-disk 100k owner for each selected evidence width. | Rollup refresh and retention-policy promotion remain #23; calendar-aware buckets remain unsupported. Retained benchmark artifact links are recorded in Production Readiness. | Selected time-series index surface promoted; rollups and retention remain Experimental. |
| #28 | The selected surface is the local materialized projection lifecycle in `docs/materialized-projection-lifecycle.md`. `tests/analytics.rs`, `tests/bench_operations.rs`, and `verification_tests` cover DML boundaries, freshness and stale fallback, interruption at row, hash, activation, metadata, and debt boundaries, restart, inactive-version drop cleanup, bounded rebuild and repair transactions, and exact metrics. Tier 5 retains native-disk 100k refresh, replay, full-verification, and hash-repair owners at one commit. CBM2 codec and DML amplification gates remain supporting dependencies. | Column-store table mode, 250k retained capacity rows, and deployment-profile alert thresholds (#8) remain outside this promotion. Retained artifact links are recorded in Production Readiness. | Selected projection lifecycle surface promoted; column-store table mode remains Experimental. |
| #29 | Local diagnostics, capacity categories, maintenance debt, repair reports, and operational assignments are documented and covered by `tests/metrics_*`, `tests/operational_*`, and projection repair suites. [Local Diagnostics Contract](local-diagnostics-contract.md) publishes the field-level contract plus a per-state operator action table (alert/retry/drain/rollback/escalation) for health, assignment, maintenance-debt, and repair-report states. `tests/storage_indexes.rs::operational_ownership` proves restart hydration, that assignment metadata never routes or filters queries, last-write-wins concurrent-update behavior, and that cardinality tracks distinct `assignment_id`s rather than write count. | Retained native-disk deployment-profile thresholds and recovery/repair/capacity evidence (disk growth, backup/restore time, rebuild/repair time, failure-injection, sustained mixed workload); routing, movement, admission, replication, and distributed repair remain external. | Local field contract and deterministic ownership/concurrency evidence closed; native-disk readiness evidence remains Open pending #8. |

The common gates are defined in [Experimental Promotion Criteria](experimental-promotion-criteria.md).
Promotion records must link exact commits, toolchains, fixture/profile ids, commands, artifacts,
and unresolved blockers in [Production Readiness](production-readiness.md).
