# Production Readiness

This matrix classifies major Cassie feature families against the bar in [Definition of Done](definition-of-done.md).
It is separate from implementation status: a feature can be implemented or stable without being production-ready.

No feature family is currently classified as production-ready by default.
Stable areas are production-ready candidates only after their benchmark, operational, restart, compatibility, and blocker evidence is complete for a declared deployment profile.
Deployment-profile benchmark output is evidence collection only; it does not create a production SLA or production-ready promotion by itself.
The Phase 10 local fallback rebaseline closed the ranked local blockers for projection refresh, graph setup, time-series scans, HTTP document writes, mixed ingest/query, and HTTP vector search; those results are advisory evidence, not production-ready promotion.
For scalar-index and read-optimization work, local-dev evidence can validate access paths,
diagnostics, and fallback behavior, but it does not establish production adaptive thresholds.
Production use still needs deployment-profile values for adaptive cost savings, feedback confidence,
operator switching thresholds, and rollback/fallback evidence.

## Readiness Levels

| Readiness | Meaning |
| --- | --- |
| Production-ready | Stable, documented, benchmarked where performance-sensitive, operationally observable, restart-safe where metadata persists, and supported boundaries are explicit. |
| Stable candidate | Stable user-visible behavior exists, but production evidence or capacity guidance is not complete. |
| Experimental baseline | Supported cases work, but compatibility, scale, output shape, or operational guarantees may change. |
| Planned | Accepted product area without an implemented production guarantee. |

## Feature Matrix

| Feature family | Owner | Support level | Readiness | Evidence | Benchmarks and signals | Restart/hydration | Blockers before production-ready |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Core SQL reads | `src/sql`, `src/planner`, `src/executor` | Stable/Experimental by semantic shape | Experimental baseline | `tests/integration_sql_*`, `tests/planner_*`, `tests/executor_*` | `perf.core_read.simple.10k`, `perf.core_read.simple.100k`, EXPLAIN access-path labels, query metrics | Schema/catalog hydration tests cover persisted metadata where relevant | Complete three-valued NULL behavior, typed operand/division errors, table-free SELECT, recursive CTE working-table semantics, window frames, deployment-profile evidence, capacity targets, and operator thresholds |
| DML and transactions | `src/sql`, `src/executor`, `src/midge` | Stable/Experimental for projection-state mutation | Experimental baseline | `tests/integration_sql_insert_values.rs`, `tests/integration_sql_update.rs`, `tests/integration_sql_delete.rs`, `tests/integration_sql_transactions.rs` | Write-path metrics, storage write counters, transaction tests | Transaction and row-blob restart paths covered by storage/catalog tests | Immediate single-collection staging enforcement, unsupported transaction/DDL preflight, safe post-commit derived refresh handling, deployment durability notes, and write-capacity guidance |
| Schema, constraints, and catalog | `src/catalog`, `src/sql`, `src/midge` | Stable/Experimental by object type | Experimental baseline | `tests/catalog_introspection.rs`, `tests/integration_sql_constraints.rs`, `tests/midge_metadata_stats.rs`, `tests/schema_operation_recovery.rs`, `tests/index_publication_recovery.rs` | Catalog probes, SQLSTATE metadata, constraint diagnostics | Restart hydration tests for schemas, constraints, indexes, views, procedures, rollups, retention, operational metadata; interrupted table/index cleanup is idempotent, rejected collection-rename intents are discarded before restart replay, collection rename moves generation/debt state with the collection and preserves destination writes during replay, collection rename publication is serialized against source-collection DML, collection drop publication and cleanup are serialized against collection DML, add-column journals replay generation-bound column-batch and projection-hash debt before clearing the intent, failed vector publication remains hidden until replay, prepared column publication rebuilds batches before visibility, index drop sidecar cleanup and metadata removal are serialized against collection DML, index sidecars are removed before metadata, graph backing-table cleanup removes outbound/inbound adjacency sidecars, and field rename/drop publication is serialized against collection DML | Remaining interruption points for every dependent sidecar/index family, concurrent schema/DML evidence, production classification by object family, and remaining catalog parity boundaries |
| Scalar indexes and read optimization | `src/planner`, `src/executor`, `src/midge` | Stable/Experimental baseline | Experimental baseline | `tests/integration_sql_scalar_indexes.rs`, `tests/integration_sql_read_path_depth.rs`, `tests/planner_read_path_depth.rs`, `tests/planner_indexes.rs`, `tests/metrics_read_paths.rs`, `tests/metrics_adaptive.rs` | `perf.core_read.simple.*`, `perf.read_path.mixed_order.*`, `perf.read_path.expression_index.*`, access-path metrics, adaptive guard/selection metrics, EXPLAIN labels | Index metadata and expression-index restart coverage covered by integration tests; adaptive feedback hydration covered separately | Complete generation-bound publication/debt audit, production adaptive cost/confidence and operator-switch thresholds, disabled/fallback evidence by deployment profile, larger-scale read evidence, and non-selective mixed-direction suffix guidance |
| Pgwire protocol and client access | `src/pgwire`, `src/sql`, `src/catalog` | Stable/Experimental | Experimental baseline | `tests/pgwire_*`, `tests/role_authorization.rs`, `tests/compatibility_matrix.rs`, `tests/compatibility_sqlalchemy.rs`, [PostgreSQL Compatibility](postgres-compatibility.md) | `perf.pgwire.simple_query.10k`, `perf.pgwire.simple_query.100k`, pgwire metrics, read-only-role SQLSTATE coverage, opt-in psql, SQLAlchemy Core, and Prisma CLI probes | Startup, auth, role-access, catalog, and session-scope/search-path probes covered | Quote/comment-aware multi-statement framing, complete binary codecs, reachable SQLSTATE coverage, plus sqlx/diesel and broader ORM/tool automation |
| Projection lifecycle and replay | `src/app`, `src/catalog`, `src/executor` | Experimental Cassie-specific | Experimental baseline | `tests/projection_lifecycle.rs`, `tests/metrics_runtime.rs`, `tests/analytical_projections.rs`, `tests/analytical_projection_recovery.rs`, [Projection Replay Contracts](projection-replay-contracts.md) | `perf.replay.lag_catchup.*`, `perf.rebuild.refresh.*`, projection metrics and catalog views | Projection metadata hydration and restart tests exist; source writes persist generation-bound materialized-projection debt atomically, fence analytical reads with `maintenance_pending`, and replay stale metadata idempotently after restart | Production replay capacity evidence and deployment-profile thresholds |
| Verification, repair, and consistency | `src/catalog`, `src/executor`, `src/rest` | Experimental baseline | Experimental baseline | `tests/projection_verification.rs`, `tests/projection_diffing.rs`, `tests/projection_consistency.rs`, `tests/projection_repair.rs`, [Projection Repair Runbook](projection-repair-runbook.md) | `perf.verification.full.*`, integrity/repair metrics and catalog reports | Persisted reports and restart hydration covered; verified column-index repair is collection-gated, audited, post-verified, and restart-safe; active materialized full-rebuild repair and explicit projection-version repair are source/output-gated, audited, activation-safe, post-verified, restart-safe; row/range projection-hash repair is collection-gated and generation-bound; a 1,024-row/four-range manifest repair is exported and reopened successfully after restart; snapshot rollback rehearsal restores the pre-repair version and audit state | Production-scale manifest read/repair capacity and deployment-profile thresholds |
| Snapshot and local restore | `src/app`, `src/midge` | Experimental baseline | Experimental baseline | `tests/snapshot_restore.rs`, [Snapshot And Restore](snapshot-restore.md) | Restore smoke tests, compatibility manifest diagnostics, copy-consistency checks, and partial-copy cleanup checks | Restore-to-new-data-dir query smoke coverage plus concurrent source-mutation rejection, restored epoch/generation/projection equality, recovery journal/debt parsing, and cleanup of failed snapshot/restore copies | Scheduling, encryption, retention, remote transport, failover, and capacity guidance remain external |
| Search, vector, and hybrid retrieval | `src/search`, `src/vector`, `src/embeddings`, `src/executor` | Stable/Experimental by path | Experimental baseline | `tests/integration_sql_fulltext_query.rs`, `tests/integration_sql_vector_*`, `tests/integration_sql_hybrid_query.rs`, `tests/hnsw_indexes.rs`, `tests/ivfflat_indexes.rs`, `tests/storage_integrity.rs`, `tests/derived_state_recovery.rs`, `tests/projection_hash_recovery.rs`, `tests/vector_state_generation.rs`, `tests/normalized_vector_generation.rs`, `tests/cardinality_generation.rs` | `perf.search.fulltext.*`, `perf.vector.executor.*`, `perf.hybrid.executor.*`, candidate/fallback metrics | Registered vector candidate state, normalized-vector sidecars, cardinality statistics, column-batch metadata, and projection-hash roots carry durable built generations; mismatches are not used. Column-batch and projection-hash maintenance debt is persisted with the write and retried at startup; column-batch reads report `maintenance_pending` and fall back until retry succeeds. | Larger corpus evidence, provider operational guidance, HNSW/IVFFlat promotion criteria |
| Analytics and time series | `src/executor`, `src/catalog`, `src/planner` | Experimental | Experimental baseline | `tests/column_batches.rs`, `tests/aggregate_acceleration.rs`, `tests/time_series_*`, `tests/analytical_projection_recovery.rs` | `perf.time_series.window_scan.*`, `perf.time_series.retention.*`, `perf.time_series.rollup_refresh.*`, rollup/retention/time-series metrics | Rollup source writes persist a generation-bound, source-scoped maintenance debt before post-commit refresh; materialized analytical source writes persist the matching `materialized_projection` debt; `maintenance_pending` fences rollup and analytical reads, `pg_catalog.pg_maintenance_debt` exposes target generation/retry/error/fallback state, and startup retries and clears the debt after rebuilding or stale-marking. Retention, column-batch, and time-series metadata hydration remain covered where applicable. | Larger analytical fixtures, exact source-generation binding during concurrent refresh, and capacity guidance |
| Operational scale metadata | `src/catalog`, `src/app`, `src/rest` | Experimental baseline | Experimental baseline | `tests/operational_ownership.rs`, [Operational Scale](operational-scale.md) | Assignment catalog diagnostics, external router contract, assignment lifecycle, and operational metadata tests | Restart hydration covered | Deployment-profile router evidence, fleet monitoring thresholds, and production runbook validation |
| Limited procedures and CALL | `src/sql`, `src/executor`, `src/catalog` | Experimental compatibility/admin surface | Experimental baseline | `tests/executor_commands.rs`, `tests/parser_functions_roles.rs`, `tests/compatibility_matrix.rs` | Procedure rejection tests and pgwire compatibility coverage | Procedure metadata hydration covered | No PL/pgSQL, triggers, dynamic SQL, transaction-control procedures, recursive workflows, or business-logic platform semantics |
| Capacity management | `src/midge`, `src/app`, `docs` | Experimental diagnostics | Experimental baseline | [Capacity Management](capacity-management.md), [Performance Contracts](performance-contracts.md), `tests/metrics_capacity.rs`, runtime metrics snapshots | Storage-family operation counts, `/metrics.capacity` advisory family/category byte totals, cache occupancy, fallback counters, column-batch byte totals, projection rebuild/write counters, manual benchmark scenarios with deployment-profile report lines | Capacity diagnostics are computed locally from Midge key/value scans and persist no separate metadata | Physical disk thresholds, admission-control policy, automatic movement, and production thresholds by deployment profile |

## Promotion Rules

- Experimental surfaces must satisfy [Experimental Promotion Criteria](experimental-promotion-criteria.md) before a future issue promotes or narrows them.
- Do not mark a feature family production-ready unless this document links to tests, benchmark or operational evidence, restart/hydration evidence when metadata persists, and explicit blockers are closed.
- Do not promote experimental surfaces solely because they are implemented or covered by integration tests.
- Performance-sensitive paths need benchmark scenario ownership, recorded deployment-profile evidence, and metrics evidence before promotion.
- Manual benchmark report lines are not CI gates unless a future issue explicitly defines a lightweight CI subset.
- Pgwire-visible behavior needs deterministic errors and SQLSTATE coverage for common failures before promotion.
- Cassie-specific operational workflows need runbook-quality docs before promotion.
